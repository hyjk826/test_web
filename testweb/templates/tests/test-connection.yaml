apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testweb.fullname" . }}-test-connection"
  labels:
    {{- include "testweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
